---

gnu_mirror_source: "ftp.gnu.org::gnu/"
gnu_mirror_cron_disabled: true
gnu_mirror_cron_minute: "{{ 59 | random(seed=inventory_hostname) }}"
gnu_mirror_cron_hour: "{{ 23 | random(seed=inventory_hostname) }}"
